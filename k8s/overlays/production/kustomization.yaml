apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: garbo

resources:
  - ../../base

patches:
  - path: ingress-patch.yaml

images:
  - name: ghcr.io/klimatbyran/garbo
    newTag: "3.6.0"
    annotations:
      $imagepolicy: flux-system:garbo
